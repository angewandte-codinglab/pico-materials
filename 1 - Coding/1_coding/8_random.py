# import random function from the random package
# randrange generates a random number between two values
from random import randrange

# generates a random number between 0 – 10, excluding 10
result = randrange(10)
print(result)

# generates a random number between 5 – 10, excluding 10
result = randrange(5, 10) 
print(result)
